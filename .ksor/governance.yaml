# The Governance Policy: who has authority over this record. `public` is implicit;
# register a restricted audience here before any document names it:
#   audiences:
#     internal:
#       description: Employees
version: "0.1"
approval_authorities:
  # The starter producer was retired when the last sample document left the
  # record — nothing of the owner's is approved by a tool.
  - actors: [human:nabeelmanjhoti]
takedown_authorities:
  actors: [human:nabeelmanjhoti]